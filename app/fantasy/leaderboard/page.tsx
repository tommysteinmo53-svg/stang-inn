"use client";

import {useEffect,useMemo,useState} from "react";
import {getSupabaseBrowserClient} from "../../../lib/supabase";
import SIIcon,{type SIIconName} from "../../../components/SIIcon";
import "../fantasy.css";

type Standing={standings_position:number;previous_standings_position:number|null;position_change:number;participant_count:number;team_id:string;team_name:string;owner_name:string;total_points:number;rounds_scored:number;round_wins:number;best_round_points:number;average_round_points:number;last_round_no:number|null;last_round_points:number|null;last_round_position:number|null;last_round_participants:number|null};
type HistoryRow={round_id:string;round_no:number;deadline_at:string;team_name?:string|null;owner_name?:string|null;round_points:number;round_position:number;booster_type?:string|null;event_type?:string|null;event_budget?:number|null};
type Monthly={month_key:string;month_start:string;standings_position:number;team_id:string;team_name:string;owner_name:string;monthly_points:number;rounds_scored:number};
type Achievement={team_id:string;current_streak:number;longest_streak:number;expert_title:string;expert_icon:string;title_reason?:string|null};
type RoundTag={label:string;icon:SIIconName};
const SEASON="2026/27";
const pts=(v:unknown)=>Number(v||0).toFixed(1).replace(".0","");
const monthLabel=(v:string)=>new Intl.DateTimeFormat("nb-NO",{month:"long",year:"numeric",timeZone:"Europe/Oslo"}).format(new Date(`${v}T12:00:00Z`));
const movement=(r:Standing)=>r.previous_standings_position==null?"Ny":r.position_change>0?`↑ ${r.position_change}`:r.position_change<0?`↓ ${Math.abs(r.position_change)}`:"→";
const roundTag=(h:HistoryRow):RoundTag|null=>h.event_type==="rich_uncle"?{label:"Rik Onkel",icon:"event"}:h.event_type==="poor_uncle"?{label:"Fattig Onkel",icon:"event"}:h.event_type==="christmas_party"?{label:"Julebord · Alle skal med!",icon:"event"}:h.booster_type==="captain_boost"?{label:"Kapteinsboost",icon:"captain"}:h.booster_type==="line_boost"?{label:"Rekkeboost",icon:"booster"}:h.booster_type==="transfer_boost"?{label:"Bytteboost",icon:"transfer"}:null;
const IconLabel=({name,children,className}:{name:SIIconName;children:React.ReactNode;className?:string})=><span className={className} style={{display:"inline-flex",alignItems:"center",gap:6}}><SIIcon name={name} size={15}/>{children}</span>;

export default function FantasyLeaderboard(){
 const[auth,setAuth]=useState<boolean|null>(null),[busy,setBusy]=useState(false),[message,setMessage]=useState("");
 const[rows,setRows]=useState<Standing[]>([]),[monthly,setMonthly]=useState<Monthly[]>([]),[achievements,setAchievements]=useState<Record<string,Achievement>>({});
 const[selected,setSelected]=useState<string|null>(null),[history,setHistory]=useState<Record<string,HistoryRow[]>>({}),[myTeamId,setMyTeamId]=useState<string|null>(null);
 useEffect(()=>{(async()=>{const sb=getSupabaseBrowserClient();if(!sb){setAuth(false);return}const{data}=await sb.auth.getSession();if(!data.session){setAuth(false);return}setAuth(true);const{data:team}=await sb.from("fantasy_user_teams").select("id").eq("season",SEASON).eq("user_id",data.session.user.id).maybeSingle();setMyTeamId(team?.id||null)})()},[]);
 async function load(){setBusy(true);setMessage("");try{const sb=getSupabaseBrowserClient();if(!sb)throw new Error("Supabase er ikke tilgjengelig");const[{data:r,error},{data:m,error:me},{data:a,error:ae}]=await Promise.all([sb.rpc("get_fantasy_competition_table_v2",{p_season:SEASON}),sb.rpc("get_fantasy_monthly_leaderboard_v2",{p_season:SEASON}),sb.rpc("get_fantasy_team_achievements",{p_season:SEASON})]);if(error)throw error;if(me)throw me;if(ae)throw ae;setRows((r||[]) as Standing[]);setMonthly((m||[]) as Monthly[]);const map:Record<string,Achievement>={};for(const x of (a||[]) as Achievement[])map[x.team_id]=x;setAchievements(map)}catch(e:any){setMessage(`Kunne ikke hente leaderboard: ${e?.message||e}`)}finally{setBusy(false)}}
 useEffect(()=>{if(auth)load()},[auth]);
 async function toggle(teamId:string){if(selected===teamId){setSelected(null);return}setSelected(teamId);if(history[teamId])return;const sb=getSupabaseBrowserClient();if(!sb)return;let data:any[]|null=null;let error:any=null;const v3=await sb.rpc("get_fantasy_team_season_history_v3",{p_team_id:teamId,p_season:SEASON});data=v3.data as any[]|null;error=v3.error;if(error){const v2=await sb.rpc("get_fantasy_team_season_history_v2",{p_team_id:teamId,p_season:SEASON});data=v2.data as any[]|null;error=v2.error}if(error){setMessage(`Kunne ikke hente rundehistorikk: ${error.message}`);return}setHistory(v=>({...v,[teamId]:(data||[]).map((x:any)=>({...x,round_no:Number(x.round_no),round_points:Number(x.round_points),round_position:Number(x.round_position),event_budget:x.event_budget==null?null:Number(x.event_budget)})) as HistoryRow[]}))}
 const winners=monthly.filter(x=>x.standings_position===1);
 const leader=rows[0],hasScoredRounds=rows.some(r=>r.rounds_scored>0),mostWins=useMemo(()=>rows.length?Math.max(...rows.map(r=>r.round_wins)):0,[rows]);
 const myTeam=rows.find(r=>r.team_id===myTeamId),myAchievement=myTeamId?achievements[myTeamId]:null,participants=leader?.participant_count??rows.length;
 if(auth===null)return <main className="fantasy-shell"><p className="fantasy-lead">Laster leaderboard …</p></main>;
 if(!auth)return <main className="fantasy-shell"><section className="team-panel"><h1>Leaderboard</h1><p className="team-muted">Du må være logget inn for å se tabellen.</p></section></main>;
 return <main className="fantasy-shell leaderboard-shell">
  <section className="team-builder-head"><div><p className="fantasy-kicker">STANG INN · EHL FANTASY 2026/27</p><h1>Leaderboard</h1><p>Achievements, streaks og kampen om Stang Inn-tittelen.</p></div><button className="leaderboard-refresh" onClick={load} disabled={busy}>{busy?"Oppdaterer …":"↻ Oppdater"}</button></section>
  <section className="team-panel" style={{marginBottom:16}}><div className="leaderboard-section-head"><div><p className="eyebrow">ACHIEVEMENTS</p><h2>Dine prestasjoner</h2></div></div><div className="fantasy-achievements-grid"><article className="fantasy-achievement-card"><span>Eksperttittel</span><strong><IconLabel name="achievement">{myAchievement?.expert_title||"Rookie"}</IconLabel></strong><p>{myAchievement?.title_reason||"Konkurransetittelen aktiveres etter minst tre scorede runder."}</p></article><article className="fantasy-achievement-card"><span>Nåværende streak</span><strong><IconLabel name="stats">{myAchievement?.current_streak||0}</IconLabel></strong><p>Antall scorede runder på rad med plassering i øvre halvdel av feltet.</p></article><article className="fantasy-achievement-card"><span>Streak-rekord</span><strong><IconLabel name="achievement">{myAchievement?.longest_streak||0}</IconLabel></strong><p>Din lengste sammenhengende streak så langt denne sesongen.</p></article></div></section>
  {winners.length>0&&<section className="team-panel leaderboard-months" style={{marginBottom:16}}><div className="leaderboard-section-head"><div><p className="eyebrow">MÅNEDSVINNERE</p><h2><IconLabel name="trophy">Månedens beste</IconLabel></h2></div></div><div className="leaderboard-winners">{winners.map(w=><article key={`${w.month_key}-${w.team_id}`}><small>{monthLabel(w.month_start)}</small><strong>{w.team_name}</strong><span className="leaderboard-owner">{w.owner_name}</span><span>{pts(w.monthly_points)} p · {w.rounds_scored} runder</span></article>)}</div></section>}
  <section className="leaderboard-summary"><article><small>Leder</small><strong>{hasScoredRounds&&leader?leader.team_name:"—"}</strong><span>{hasScoredRounds&&leader?`${leader.owner_name} · ${pts(leader.total_points)} poeng`:"Sesongen har ikke startet"}</span></article><article><small>Mitt lag</small><strong>{hasScoredRounds&&myTeam?`${myTeam.standings_position}. av ${participants}`:"—"}</strong><span>{hasScoredRounds&&myTeam?`${movement(myTeam)} siden forrige runde`:myTeam?"Venter på første scorede runde":"Ingen plassering ennå"}</span></article><article><small>Deltakere</small><strong>{participants}</strong><span>{leader?.last_round_no?`Siste scorede: runde ${leader.last_round_no}`:"Ingen scorede runder ennå"}</span></article><article><small>Flest rundeseire</small><strong>{mostWins}</strong><span>{leader?.rounds_scored?`${leader.rounds_scored} scorede runder`:"Ingen avgjort ennå"}</span></article></section>
  {message&&<p className="team-error">{message}</p>}
  <section className="team-panel leaderboard-table-panel"><div className="leaderboard-section-head"><div><p className="eyebrow">SESONGTABELL</p><h2>Stillingen</h2></div><span className="team-muted">{participants} deltakere · trykk på et lag for rundehistorikk</span></div>{rows.length===0?<div className="leaderboard-empty"><strong>Ingen fantasy-lag ennå.</strong><span>Tabellen fylles automatisk når lag opprettes og rundene scores.</span></div>:<div className="leaderboard-list"><div className="leaderboard-columns"><span>Plass</span><span>Lag / eier</span><span>Poeng</span><span>Seire</span><span>Snitt</span><span>Siste</span><span/></div>{rows.map(r=>{const a=achievements[r.team_id],mine=r.team_id===myTeamId;return <div className={`leaderboard-entry ${hasScoredRounds&&r.standings_position<=3?"podium":""} ${mine?"mine":""}`} key={r.team_id}><button className="leaderboard-row" onClick={()=>toggle(r.team_id)}><span className="leaderboard-rank">{hasScoredRounds?`${r.standings_position}.`:"—"}{hasScoredRounds&&<small style={{display:"block",fontSize:11,marginTop:3}}>{movement(r)}</small>}</span><span className="leaderboard-team"><strong>{r.team_name}{mine&&<em>MITT LAG</em>}</strong><small className="leaderboard-owner">{r.owner_name}</small><small><IconLabel name="achievement">{a?.expert_title||"Rookie"}</IconLabel> · <IconLabel name="stats">streak {a?.current_streak??0}</IconLabel></small></span><span className="leaderboard-points"><small>Poeng</small><b>{pts(r.total_points)}</b></span><span><small>Seire</small><b>{r.round_wins}</b></span><span><small>Snitt</small><b>{pts(r.average_round_points)}</b></span><span><small>{r.last_round_no?`R${r.last_round_no}`:"Siste"}</small><b>{r.last_round_no?`${pts(r.last_round_points)} p`:"—"}</b>{r.last_round_position&&<small style={{display:"block"}}>{r.last_round_position}. av {r.last_round_participants}</small>}</span><span className="leaderboard-chevron">{selected===r.team_id?"▾":"▸"}</span></button>{selected===r.team_id&&<div className="leaderboard-history"><div className="leaderboard-history-title"><div><strong><IconLabel name="achievement">{a?.expert_title||"Rookie"}</IconLabel></strong><span><IconLabel name="stats">Nåværende streak {a?.current_streak??0} · rekord {a?.longest_streak??0}</IconLabel></span></div><div><strong>Beste runde {pts(r.best_round_points)} p</strong><span>{r.round_wins} rundeseire{hasScoredRounds?` · sammenlagt ${r.standings_position}. av ${r.participant_count}`:" · sesongen har ikke startet"}</span></div></div>{(history[r.team_id]||[]).length===0?<p className="team-muted">Ingen scorede runder ennå.</p>:<div className="leaderboard-history-grid">{history[r.team_id].map(h=>{const tag=roundTag(h);return <article key={h.round_id}><span>Runde {h.round_no}</span><strong>{pts(h.round_points)} p</strong><small>{h.round_position===1?<IconLabel name="trophy">Rundeseier</IconLabel>:`${h.round_position}. plass`}</small>{h.team_name&&<small className="leaderboard-history-identity">{h.team_name} · {h.owner_name||"Ukjent spiller"}</small>}{tag&&<small style={{display:"block",marginTop:5,fontWeight:800,color:"#d8b65e"}}><IconLabel name={tag.icon}>{tag.label}{h.event_budget?` · ${pts(h.event_budget)}m`:""}</IconLabel></small>}</article>})}</div>}</div>}</div>})}</div>}</section>
 </main>
}
