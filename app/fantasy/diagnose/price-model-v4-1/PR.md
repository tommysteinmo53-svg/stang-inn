# Pull request notes

This branch intentionally adds v4.1 as a separate diagnostic route rather than replacing v4 immediately. That makes side-by-side comparison possible before the new market calibration is promoted to the primary model.
