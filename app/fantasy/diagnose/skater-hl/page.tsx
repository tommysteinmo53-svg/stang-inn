"use client";
import {useEffect,useMemo,useState} from "react";
import {getSupabaseBrowserClient} from "../../../../lib/supabase";
import "../../fantasy.css";

const sign=(v:number)=>v>0?`+${v}`:String(v);
export default function SkaterHL(){const[data,setData]=useState<any>(null),[msg,setMsg]=useState("Laster …");useEffect(()=>{(async()=>{try{const s=getSupabaseBrowserClient();const{data:session}=await s!.auth.getSession();const token=session.session?.access_token;if(!token)throw new Error("Du må være logget inn som admin.");const res=await fetch("/api/fantasy-skater-season-check?season=2025%2F26&tournamentId=435587",{headers:{Authorization:`Bearer ${token}`}}),p=await res.json();if(!res.ok||!p.ok)throw new Error(p.error||"Avstemming feilet");setData(p);setMsg("")}catch(e:any){setMsg(e.message||"Feil")}})()},[]);const rows=useMemo(()=>data?.result||[],[data]);return <main className="fantasy-shell"><section className="fantasy-hero"><div><p className="fantasy-kicker">STANG INN · ADMIN · FANTASY</p><h1>Utespiller · HockeyLive-avstemming</h1><p className="fantasy-lead">Sammenligner summerte kampdata mot HockeyLive sin sesongrad. HockeyLive er fasit for råstatistikk.</p></div><a className="pill" href="/fantasy/diagnose/skaters" style={{textDecoration:"none"}}>← FP-regeldiagnose</a></section><section className="fantasy-card"><div style={{padding:12,border:"1px solid #b7d7c4",borderRadius:12,background:"#eefaf2",marginBottom:14}}><b>Kontroll:</b> G, A, SOG, +/− og PIM sammenlignes. Kampantall vises bare diagnostisk. 19Fantasy brukes ikke til å overstyre HockeyLive.</div>{msg&&<p>{msg}</p>}{data&&<><div style={{display:"flex",gap:18,flexWrap:"wrap",marginBottom:14}}><b>HockeyLive-rader: {data.officialCount}</b><b>Navn matchet: {data.matched}/{rows.length}</b><b>Alle råfelt like: {data.clean}/{rows.length}</b></div><div style={{overflowX:"auto"}}><table style={{width:"100%",borderCollapse:"collapse",minWidth:1250,fontSize:12}}><thead><tr>{["Spiller","Pos","K vår/HL*","ΔK*","G vår/HL","ΔG","A vår/HL","ΔA","SOG vår/HL","ΔSOG","+/− vår/HL","Δ+/−","PIM vår/HL","ΔPIM","Status"].map(h=><th key={h} style={{padding:6,textAlign:"left",background:"#eef3f8"}}>{h}</th>)}</tr></thead><tbody>{rows.map((r:any)=>{const l=r.local,o=r.official,d=r.diffs;return <tr key={l.id} style={{background:r.clean?"#eaf8ef":r.matched?"#fff4df":"#f8fafc"}}><td style={{padding:6,borderBottom:"1px solid #e2e8f0",fontWeight:800}}>{l.name}</td><td>{l.position}</td>{o?<><td>{l.games}/{o.games}</td><td style={{color:"#607086",fontWeight:700}}>{sign(d.games)}</td><td>{l.goals}/{o.goals}</td><td style={{fontWeight:800,color:d.goals===0?"#16794b":"#a23b2a"}}>{sign(d.goals)}</td><td>{l.assists}/{o.assists}</td><td style={{fontWeight:800,color:d.assists===0?"#16794b":"#a23b2a"}}>{sign(d.assists)}</td><td>{l.shots}/{o.shots}</td><td style={{fontWeight:800,color:d.shots===0?"#16794b":"#a23b2a"}}>{sign(d.shots)}</td><td>{l.plusMinus}/{o.plusMinus}</td><td style={{fontWeight:800,color:d.plusMinus===0?"#16794b":"#a23b2a"}}>{sign(d.plusMinus)}</td><td>{l.pim}/{o.pim}</td><td style={{fontWeight:800,color:d.pim===0?"#16794b":"#a23b2a"}}>{sign(d.pim)}</td><td style={{fontWeight:800,color:r.clean?"#16794b":"#a23b2a"}}>{r.clean?"✓ Matcher HL":"⚠ Avvik"}</td></>:<td colSpan={13} style={{color:"#a23b2a",fontWeight:700}}>Fant ikke HockeyLive-rad</td>}</tr>})}</tbody></table></div><p style={{fontSize:12,color:"#607086"}}>* Kampantall påvirker ikke status.</p></>}</section></main>}
