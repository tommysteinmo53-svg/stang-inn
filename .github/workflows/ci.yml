name: MP-01 onboarding probe

on:
  pull_request:
    branches: [main]

jobs:
  onboarding:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22
      - name: Install dependencies
        run: npm install
      - name: MP-01.7 onboarding regression
        run: npm run test:mp01:onboarding
