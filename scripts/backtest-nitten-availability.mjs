const URL="https://www.nitten.no/blogg/for-dropp-halmrast-tilbake-kaijser-ma-sone-ingen-gloppen-straff";
const expected={"Andreas Dahl":"out","Stian Kaltrud Nystuen":"returning","Sondre Olden":"out","Leo Johansen Halmrast":"returning","Didrik Utter":"out","Stian Nybraaten Hansen":"out","Sebastian Kaijser":"out","Martin Grönberg":"out","Jonas Nyhus Myhre":"out","Simen Ahlsen":"out","Filip Lalande":"out","Sebastian Johansen":"out","Linus Pettersson":"returning","Lars Christian Rødne":"long_term","Håkon Løken Pedersen":"long_term","Ludvig Hoff":"long_term","Martin Gran":"long_term","Andrew Yogan":"out"};
const html=await fetch(URL,{headers:{"User-Agent":"StangInnAvailabilityBacktest/1.0"}}).then(r=>{if(!r.ok)throw new Error(`${r.status} ${r.statusText}`);return r.text()});
const text=html.replace(/<script\b[^>]*>[\s\S]*?<\/script>/gi," ").replace(/<style\b[^>]*>[\s\S]*?<\/style>/gi," ").replace(/<[^>]+>/g," ").replace(/&nbsp;|&#160;/gi," ").replace(/&amp;/gi,"&").replace(/\s+/g," ").trim();
const sentenceFor=(name)=>{const lower=text.toLocaleLowerCase("nb-NO"),index=lower.indexOf(name.toLocaleLowerCase("nb-NO"));if(index<0)return null;const start=Math.max(0,Math.max(text.lastIndexOf(".",index-1),text.lastIndexOf("!",index-1),text.lastIndexOf("?",index-1))+1),ends=[text.indexOf(".",index),text.indexOf("!",index),text.indexOf("?",index)].filter(v=>v>=0),end=ends.length?Math.min(...ends)+1:Math.min(text.length,index+260);return text.slice(start,end).trim()};
const classify=(sentence,name)=>{if(!sentence)return null;const s=sentence.toLocaleLowerCase("nb-NO"),n=name.toLocaleLowerCase("nb-NO"),at=s.indexOf(n),local=s.slice(Math.max(0,at-80),Math.min(s.length,at+n.length+120));if(/tilbake/.test(local))return"returning";if(/ute for sesongen|ute resten av sesongen|langtidssk/.test(local))return"long_term";if(/karantene|soner|skadet|skadd|fortsatt (?:er )?ute|har vært ute lenge|har manglet|mangler/.test(local))return"out";if(/usikker|tvilsom|dag til dag/.test(local))return"questionable";return null};
let pass=0;for(const [name,want] of Object.entries(expected)){const sentence=sentenceFor(name),got=classify(sentence,name),ok=got===want;if(ok)pass++;console.log(`${ok?"PASS":"FAIL"} ${name}: expected=${want} got=${got??"none"}${sentence?` | ${sentence}`:""}`)}console.log(`\n${pass}/${Object.keys(expected).length} golden expectations passed`);if(pass!==Object.keys(expected).length)process.exitCode=1;
